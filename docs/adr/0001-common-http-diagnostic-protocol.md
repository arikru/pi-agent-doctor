# Adapters target a common HTTP diagnostic protocol

pi-agent-doctor adapters target a common HTTP diagnostic protocol rather than adding framework-specific clients to the Pi extension. This keeps the extension framework-agnostic while allowing supported adapters and project-local adapters to evolve independently.
