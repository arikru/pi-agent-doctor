---
description: Remove a diagnostic agent endpoint
argument-hint: <name>
---

Call the `agent_disconnect` tool on the `agent-doctor` MCP server with name: $ARGUMENTS

Report the tool's result back to the user in one short sentence.
