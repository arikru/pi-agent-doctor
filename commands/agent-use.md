---
description: Select the active diagnostic agent
argument-hint: <name>
---

Call the `agent_use` tool on the `agent-doctor` MCP server with name: $ARGUMENTS

Report the tool's result back to the user in one short sentence.
