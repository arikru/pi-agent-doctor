---
description: Show the diagnostic trace of the most recent agent run
---

Call the `agent_last_trace` tool on the `agent-doctor` MCP server (no arguments) and relay the trace to the user verbatim, preserving its markdown structure.
