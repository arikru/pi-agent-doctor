---
description: List configured diagnostic agents
---

Call the `agent_list` tool on the `agent-doctor` MCP server (no arguments) and show the user the resulting list verbatim. The `*` marker indicates the active agent.
